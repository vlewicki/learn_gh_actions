[project]
name = "learn-gh-actions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aeek True", email = "aeektrue@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
learn-gh-actions = "learn_gh_actions:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
